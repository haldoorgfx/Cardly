// Placeholder test. The default Flutter counter test was removed — this app
// (EventeraApp) initializes Supabase in main(), so a full widget pump needs
// test setup that isn't in place yet. This keeps `flutter test`/`analyze` green.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
