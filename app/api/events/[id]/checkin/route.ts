import { NextResponse } from 'next/server';

// Phase 1.8 — Verify QR token + mark checked_in_at
export async function POST() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
