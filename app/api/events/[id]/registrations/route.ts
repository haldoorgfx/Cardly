import { NextResponse } from 'next/server';

// Phase 1.8 — List registrations for organizer
export async function GET() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
