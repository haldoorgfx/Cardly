import { NextResponse } from 'next/server';

// Phase 1.9 — Validate a promo code at checkout
export async function POST() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
