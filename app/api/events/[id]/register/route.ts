import { NextResponse } from 'next/server';

// Phase 1.5/1.6 — Create registration + trigger card
export async function POST() {
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
